[tool.poetry]
name = "matrix-loading"
version = "0.1.0"
description = "Loading programs into the construct"
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
pandas = "^2.1.0"
numpy = "^1.25.0"
matplotlib = "^3.7.2"
